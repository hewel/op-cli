OPENPROJECT_URL=https://openproject.example.com
OPENPROJECT_TOKEN=replace-with-personal-api-token
OPENPROJECT_AUTH_MODE=bearer
OPENPROJECT_DEFAULT_PROJECT=example-project
# OPENPROJECT_ALLOW_WRITE is intentionally not loaded from .env; set it in the real process environment for writes.
